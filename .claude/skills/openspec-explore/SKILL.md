---
name: openspec-explore
description: Enter explore mode - a thinking partner for exploring ideas, investigating problems, and clarifying requirements. Use when the user wants to think through something before or during a change.
---

This skill has moved to the [agentic-plugins-marketplace](https://github.com/hmcts/agentic-plugins-marketplace) as part of the `openspec` plugin (bundled with `openspec-propose`, `openspec-apply-change`, and `openspec-archive-change`).

Install with:

```
/plugin install openspec@agentic-plugins-marketplace
```

**Prerequisite**: the `openspec` CLI must be on your `PATH`.

The `/opsx:explore` slash command in this repo still works and contains the same logic inline — the skill and the command are parallel implementations.
