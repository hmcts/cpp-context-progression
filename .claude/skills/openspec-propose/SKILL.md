---
name: openspec-propose
description: Propose a new change with all artifacts generated in one step. Use when the user wants to quickly describe what they want to build and get a complete proposal with design, specs, and tasks ready for implementation.
---

This skill has moved to the [agentic-plugins-marketplace](https://github.com/hmcts/agentic-plugins-marketplace) as part of the `openspec` plugin (bundled with `openspec-explore`, `openspec-apply-change`, and `openspec-archive-change`).

Install with:

```
/plugin install openspec@agentic-plugins-marketplace
```

**Prerequisite**: the `openspec` CLI must be on your `PATH`.

The `/opsx:propose` slash command in this repo still works and contains the same logic inline — the skill and the command are parallel implementations.
